# PROMPT 6: The Red Team Pass

"You just completed this task: [paste AI's previous output]

Now switch roles.

You are now a hostile critic whose job is to find every hallucination, exaggeration, and unsupported claim in that response.

Be ruthless. Mark every problem with [FLAG].

After the critique, produce a clean version with every flagged section fixed or removed."

Let the model debug itself before it reaches you. 